{
  "name": "server",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "start": "NODE_ENV=development node db-data/seeder.js && nodemon --inspect ./bin/www",
    "test": "jest --watch"
  },
  "jest": {
    "testEnvironment": "node"
  },
  "dependencies": {
    "cookie-parser": "~1.4.4",
    "debug": "~2.6.9",
    "express": "~4.16.1",
    "mongoose": "^6.0.5",
    "morgan": "~1.9.1"
  },
  "devDependencies": {
    "faker": "^5.5.3",
    "jest": "^27.1.1",
    "mongodb-memory-server": "^7.4.0",
    "nodemon": "^2.0.12",
    "supertest": "^6.1.6"
  }
}
